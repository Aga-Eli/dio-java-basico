package aula_de_uml;

public class uml {
    /*
     * Agregação: setinha com o início não preenchido (Uma classe pode 
     * ter relação com a outra) != Composição: setinha com o início preenchido 
     * (Uma classe precisa ter uma relação com a outra)
     */

    /*
     * No software de UML (astah; Lucidchart; draw.io; StarUML): 
     * + : Público
     * # : protected
     * - : privado
     * 
     * Precisa colocar os atributos (se possui) e as operações/métodos
     * (se possui): Primeiro a visibilidade; nome do atributo/método;
     * parâmetro entre parenteses se precisar; dois ponto (:); o tipo
     * de variável que ele retorna ou void
     * 
     * 0 : precisa de nenhum elemento
     * 1 : precisa de um elemento
     * * : precisa de vários elementos (pode ser substituido
     * pelo número de elementos)
     * 
     * Além do nome da associação
     */
}
